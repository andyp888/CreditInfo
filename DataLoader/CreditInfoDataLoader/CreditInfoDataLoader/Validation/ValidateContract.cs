using CreditInfoDataLoader.Model;
using System;
using System.Collections.Generic;
using System.Text;

namespace CreditInfoDataLoader.Validation
{
    public class ValidateContract
    {
        public bool ValidiateContract(Contract contract)
        {
            //Do validation on contract

            //If no errors found then return true, else return false (fail validation)

            return true;
        }
    }
}
